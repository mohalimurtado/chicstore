import 'package:flutter/material.dart';

class AppColors {
  // Warna Utama (Orange)
  static const Color primary = Color(0xFFFF9800); // Orange
  static const Color primaryDark = Color(0xFFF57C00); // Sedikit lebih gelap

  // Warna Netral
  static const Color background = Color(0xFFFFFFFF); // White
  static const Color text = Color(0xFF000000); // Black

  // Warna Aksen
  static const Color secondaryText = Color(
    0xFF757575,
  ); // Abu-abu untuk teks sekunder
  static const Color buttonText = Color(0xFFFFFFFF); // Teks tombol (putih)
}
